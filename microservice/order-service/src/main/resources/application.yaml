server:
  port: 8082

spring:
  datasource:
    url: jdbc:h2:mem:orderdb
    driverClass: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: order-service
